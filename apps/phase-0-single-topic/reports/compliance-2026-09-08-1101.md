# Build-compliance report — phase-0-single-topic (component update)

- **Spec:** `apps/phase-0-single-topic/specs/`
- **Build mode:** COMPONENT UPDATE — `COMPONENTS = ["components/input-mode-dual.yaml", "components/state-topic-refraction.yaml"]`. Project already exists at `apps/phase-0-single-topic/output/`; only code responsible for these two components was touched.
- **Config:** `apps/phase-0-single-topic/prompts/build-config.yaml` — `defaultFixture` block → `fixtures/promotions/`.
- **Fixture:** `FIXTURE_DIR = fixtures/promotions/`.
- **Generated:** 2026-09-08 11:01 (local)
- **Verification:** `npm test` → **37/37 pass** (Node 22.23.2) — promotions parity + `default-fixture.test.mjs` + a new `serialize-freetext.test.mjs` (6 assertions). `npm run build` (production) → **clean, ~260 kB initial**. Browser (headless): on first load the Sources and Personas steps show the pre-populated content in **both** the structured form and the Free-text tab; toggling Form↔Free-text round-trips the content losslessly.

## Delta vs. the previous report (`compliance-2026-09-08-1052.md`)

`input-mode-dual.yaml` gained one `micro` rule this run:
**`pre-populated-state-renders-in-both-modes`**. Everything else in both
components is unchanged from the 10:52 build and re-verified below.

| File | Component | Change |
|---|---|---|
| `src/app/core/serialize-freetext.ts` (new) | `input-mode.dual` | The inverse of `parse-freetext.ts`: `serializeSources` / `serializePersonas` turn state back into the same markdown `mode-parity` defines, so `parse(serialize(x)) === x`. Pure, round-trip-tested. |
| `src/app/steps/sources-step.ts` | `input-mode.dual` | `freetext` seeded from `serializeSources(store.sources())`; new `setMode()` re-serializes on entering Free-text and re-parses on entering Form, keeping both views in sync. |
| `src/app/steps/personas-step.ts` | `input-mode.dual` | Same pattern with `serializePersonas` / `parsePersonasFreetext`. |
| `tests/serialize-freetext.test.mjs` (new) | `input-mode.dual` | 6 round-trip / non-empty assertions, incl. against the default-fixture state. |

---

## components/input-mode-dual.yaml — full rule check

| Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|
| mustNever | Two modes must not produce divergent data | `src/app/core/parse-freetext.ts` + `src/app/core/serialize-freetext.ts` + `steps/sources-step.ts` / `personas-step.ts` | Both modes write via `store.setSources` / `store.setPersonas`. The serializer is the exact inverse of the parser — `tests/serialize-freetext.test.mjs` "parse(serialize(state)) deep-equals state" for both sources and personas. | Confirmed |
| mustNever | Don't split one labeled record into multiple entries | `parse-freetext.ts` `parseSourcesFreetext` → `sources: [one]`; `serialize-freetext.ts` `serializeSources` emits one record | `tests/parse-freetext.test.mjs` "one labeled record stays ONE source". | Confirmed |
| mustNever | Don't block advancement after a successful free-text parse | `steps/*-step.ts` shared `next()` | Unchanged; `setMode` never gates `next()`. | Confirmed |
| mustNever | Don't infer/add a field value not stated in free-text | `parse-freetext.ts` `asDimensionLine` / `parseSourcesFreetext`; `serialize-freetext.ts` emits only stored values (no defaults) | `tests/parse-freetext.test.mjs` "parse-only-what-is-stated" (2 cases); `tests/serialize-freetext.test.mjs` "a persona with no dimensions serializes without a trailing dimension line". | Confirmed |
| mustNever | No form field without a visible label AND placeholder | `steps/sources-step.ts` / `personas-step.ts` | Unchanged. | Confirmed |
| mustNever | Don't keep `##` / structural quotes in a parsed value | `parse-freetext.ts` `h2Sections` + `flatten` | `tests/parse-freetext.test.mjs` checks no `##` / wrapping quotes on the promotions parse. | Confirmed |
| mode-parity | Free text parses into the exact fields the form writes | `parse-freetext.ts` + `serialize-freetext.ts` | `tests/parse-freetext.test.mjs` parity assertions (promotions) + `tests/serialize-freetext.test.mjs` round-trips. | Confirmed |
| **pre-populated-state-renders-in-both-modes** | When state is pre-populated, BOTH the form AND the free-text view must display it; the textarea content is the reverse-serialization of the same state into markdown, generated at render time | `src/app/core/serialize-freetext.ts`; `src/app/steps/sources-step.ts` (`freetext = serializeSources(this.store.sources())`, `setMode`); `src/app/steps/personas-step.ts` (`freetext = serializePersonas(this.store.personas())`, `setMode`) | The free-text textarea is initialized from `serialize(current state)` at component construction, and `setMode('text')` re-serializes from the live form fields each time the tab is opened — so it is generated from state at render time, never a stale copy. `setMode('form')` re-parses the markdown so nothing is lost either way. `tests/serialize-freetext.test.mjs`: "the default-fixture pre-populated state survives a serialize → parse round trip", "serialized markdown actually contains the pre-populated values (not blank)". Browser: on first load, Sources → Free text shows `## Title / ## Source / ## Description` with the promotions values; Personas → Free text shows both `## <name>` blocks with their summaries and `Context, Trust` / `Content, Time` lines; Form↔Free-text toggles preserve content. | Confirmed |
| sources-format | `## Title` / `## Source` / `## Description` H2 sections | `parse-freetext.ts` `parseSourcesFreetext`; `serialize-freetext.ts` `serializeSources` emits exactly those three H2 sections | Parity against `fixtures/promotions/sources-freetext-markdown.md`; round-trip test. | Confirmed |
| personas-format | `## <name>` = name; next paragraph = summary; trailing comma-list vs `system.yaml` fixedDimensions = dimensions, matched by name | `parse-freetext.ts` `parsePersonasFreetext` + `asDimensionLine`; `serialize-freetext.ts` `serializePersonas` emits `## <name>`, blank, summary, blank, `Dim, Dim` | Parity against `fixtures/promotions/personas-freetext.md`; round-trip test; browser check of the seeded markdown. | Confirmed |
| parity-check-required | Run the parser against `fixtures/promotions/` and confirm it matches `expected-parsed.yaml` exactly | `tests/parse-freetext.test.mjs` (FIX → `fixtures/promotions`) | Runs in `npm test`; all parity assertions pass. | Confirmed |
| advance-after-parse | Continue behaves identically to the form path after a valid parse | `steps/*-step.ts` shared `next()` | Unchanged. | Confirmed |
| parse-only-what-is-stated | Never infer/guess/auto-complete a field not stated | `parse-freetext.ts` | `tests/parse-freetext.test.mjs` (2 cases). The serializer likewise emits only stored values and omits the dimension line entirely when there are none. | Confirmed |
| field-labels-and-placeholders | Every form field shows a visible label + placeholder | `steps/sources-step.ts` / `personas-step.ts` | Unchanged. | Confirmed |
| clean-value-extraction | Parsed values contain only the value | `parse-freetext.ts` `flatten` + `h2Sections` | `tests/parse-freetext.test.mjs`. | Confirmed |
| downloadable-template | Free-text mode offers a downloadable markdown template (headers only) | `src/app/ui/freetext-template.ts` + the "Download template" button in both steps | Unchanged. | Confirmed |

---

## components/state-topic-refraction.yaml — full rule check

| Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|
| mustNever | Lose or overwrite author-entered data between steps | `src/app/core/session-store.ts` | Unchanged from the 10:52 build. Seeding happens once in the constructor before any author entry. `setMode` in the step components edits only local component fields and re-parses/re-serializes losslessly — it never discards content. | Confirmed |
| mustNever | Persist data beyond the current session | `src/app/core/session-store.ts` | Unchanged — signals only, no storage APIs; the seed is re-derived from bundled constants each load. | Confirmed |
| fields | topicText / sources / personas (max 2) / refractedOutputs map | `src/app/core/models.ts`, `session-store.ts` | Unchanged. | Confirmed |
| initializes-from-default-fixture | On first load (and Start-over) Steps 1–3 pre-populated from `build-config.yaml`'s `defaultFixture`; a starting value, editable/replaceable; no validation difference | `src/app/core/default-fixture.ts`; `session-store.ts:28-30, 114-119, 140` | Unchanged from the 10:52 build and still verified by `tests/default-fixture.test.mjs` (6 assertions) and the browser smoke. This run additionally confirms the pre-populated state now reaches the Free-text view as well as the form (see `input-mode.dual.pre-populated-state-renders-in-both-modes`). | Confirmed |
| persona-id-is-positional | persona-0 / persona-1 by authoring order, assigned once | `session-store.ts` `setPersonas` + `nextId()` | Unchanged. | Confirmed |
| refracted-output-map | Keyed by personaId, one entry per persona, never merged | `session-store.ts` `putRefraction` | Unchanged. | Confirmed |

---

## Other components (not in COMPONENTS this run)

`layout.three-panel`, `gating.linear-sequential`, `byok-compiler.contract`,
`interrupt.conditional-api-key`, `check.grounding`, `delivery.request-response`,
`step.introduction`, `branding.rename`, `style.visual-theme` — not modified, not
re-verified this run. Last full check:
`apps/phase-0-single-topic/reports/compliance-2026-09-07-1143.md` (all Confirmed;
two "Present but unverified": a live model call, the style-reference inspection).

---

## Overall verdict

**Compliant.** The new rule
`input-mode.dual.pre-populated-state-renders-in-both-modes` is implemented as a
pure serializer that is the exact inverse of the free-text parser, seeded into
each step's Free-text textarea at render time and kept in sync on every
Form↔Free-text toggle. All other rules in both in-scope components remain
Confirmed. `37/37` tests pass, the production build is clean, and the both-modes
behaviour was exercised in a real browser. **No "Not found" or "Conflicting" rows.**
