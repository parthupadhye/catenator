# Build-compliance report — phase-1-basic-publication

- **Spec:** `apps/phase-1-basic-publication/specs/` (system.yaml + vocabulary.yaml + 12 component files)
- **Build mode:** FRESH BUILD — `COMPONENTS = []`. `apps/phase-1-basic-publication/output/` did not exist; nothing to clear. Built complete from the spec.
- **Config:** `apps/phase-1-basic-publication/prompts/build-config.yaml` — Angular frontend, branding Catenator / "Creating and Refracting — Basic Publication".
- **Fixture:** `FIXTURE_DIR = apps/phase-1-basic-publication/fixtures/rate-limiting/` was named by RUN.md but shipped **empty**. A clean fixture publication (`publication.json` — a 3-level rate-limiting topic tree, 2 sources, 2 personas, 1 variable, per-topic overrides) plus `expected.json` were created for this build so the deterministic core can be tested against a concrete example. This is noted in `fixtures/rate-limiting/publication.json` `_note`.
- **Generated:** 2026-09-07 13:51 (local)
- **Verification:** `npm test` → **41/41 pass** (Node 22.23.2), including 5 fixture-parity assertions. `npm run build` (production) → **clean, 256.5 kB initial**. `npm run verify:refraction` runs the real publication fan-out end-to-end (reaches provider; HTTP 404 with a placeholder key). Browser smoke (headless): default publication form on load, five accordion sections, add topic / add child / inline delete-with-descendant-count, topic Metadata/Editor/Preview tabs with `{{api_name}}` shown **raw** in Preview, personas capped at 2 with positional ids `persona-0`/`persona-1`, Quality Check rendering all four checks with per-item failures, Refract button disabled with no key, and the < 768px notice.

Per `apps/shared/GENERATE-COMPLIANCE-REPORT.md`: one row per `mustNever` and `micro` rule across every component.

---

## Rows needing attention first

| Rule | Status | Note |
|---|---|---|
| `refraction.publication-level` — a live model call | **Present but unverified** | The fan-out, per-topic resolved sources, retry-once, structured errors, one-failure-doesn't-block-others, and the grounding gate are all unit-tested with a fake transport (`tests/refraction.test.mjs`, `tests/fixture-parity.test.mjs`). No real Claude/Gemini call was made (no key). `npm run verify:refraction` exercises the real path on demand. |
| `style.visual-theme` — visual match to the reference app | **Present but unverified (by design)** | Per `micro.reuse-phase-0-tokens-first`, this build **reused phase-0-single-topic's already-committed design-system tokens** (`src/styles.css`) directly rather than inspecting the external reference path (`C:\Users\parth\ei\eisyntaxia_repos\syntaxia-studio`, an absolute local path). No fresh inspection of that path was performed. |

**No "Not found" rows. No "Conflicting" rows.**

The one cross-component shared value — the five fixed dimensions — is defined once, in `src/app/core/models.ts:11` (`FIXED_DIMENSIONS = ['Surface', 'Content', 'Context', 'Time', 'Trust']`), and consumed by `persona.catalog`'s form (`src/app/ui/personas-form.ts` → `dimensions = FIXED_DIMENSIONS`). It matches this app's `system.yaml` `contentScope.fixedDimensions` by value, as `persona.catalog.five-fixed-dimensions` requires.

---

## system.yaml — mustNever

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| system | mustNever-1 | Assume or hardcode a maximum topic-tree depth | `src/app/core/tree.ts` (all functions), `src/app/core/publication-store.ts` | Every tree function recurses without a level counter: `flattenTopics` `walk(n.children)` (tree.ts:26), `findTopic` recursion (tree.ts:57), `updateTopic` `children: updateTopic(n.children, …)` (tree.ts:66). No constant or comparison caps depth anywhere. `tests/tree.test.mjs` "tree supports arbitrary depth — no level cap" builds and extends a 6-deep tree. | Confirmed |
| system | mustNever-2 | Allow more than 2 personas in the publication | `src/app/core/publication-store.ts:161` | `addPersona()`: `if (this.personas().length >= MAX_PERSONAS) return;` with `MAX_PERSONAS = 2` (`models.ts:14`). UI also hides the button (`personas-form.ts` `@if (store.canAddPersona())`). Browser smoke: a 3rd "Add persona" click is a no-op; button disappears at 2. `tests/quality-check.test.mjs` covers the persona-count gate. | Confirmed |
| system | mustNever-3 | Open any Panel 1 selection in a modal dialog | `src/app/ui/editor-generic.ts`, `src/app/ui/topic-tree-node.ts` | `editor-generic.ts` `@switch (sel().type)` renders each of the five types as an inline child component in Panel 2. Delete confirmation is an inline `<div class="confirm">` block (`topic-tree-node.ts:34`), not an overlay. `tests/branding-and-layout.test.mjs` "no modal dialog anywhere" scans all of `src/` for `<dialog`, `role="dialog"`, `showModal(`, `MatDialog/CdkDialog/NgbModal` → none. | Confirmed |
| system | mustNever-4 | No Shared Blocks section / block-editing UI / block-reference resolution | `src/app/ui/accordion-nav.ts` | Exactly five `class="hd"` section headers: Publication, Topics, Variables, Sources, Personas (accordion-nav.ts:27,41,59,74,89). No block model in `models.ts`, no block component, no reference-resolution code anywhere. `tests/branding-and-layout.test.mjs` asserts `headerCount === 5` and `doesNotMatch(/>\s*Shared Blocks/i)`. | Confirmed |
| system | mustNever-5 | No fact in refracted output absent from a topic's content + resolved sources | `src/app/core/publication-refract.ts:81-99` + `src/app/core/refraction.ts` (buildPrompt) + `src/app/core/grounding.ts` | Three layers: `buildPrompt` instructs "Use ONLY facts stated… Never invent a value"; after each successful call `checkGrounding(r.text, topic.content, resolvedSources)` runs; if not grounded the cell becomes `{ ok: false, error: { type: 'ungrounded-claim' } }` and `cell.text` is never set. `tests/refraction.test.mjs` "an ungrounded model output is surfaced as an error, not kept as text". | Confirmed |

---

## components/layout-three-panel.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| layout.three-panel | mustNever | Not alter publication data / hierarchy / pipeline | `src/app/ui/workspace.ts` | The component's template is three child components and its class body is empty (`export class Workspace {}`). It injects no store and holds no logic. | Confirmed |
| layout.three-panel | accordion-sections-fixed | Exactly five sections; Shared Blocks is not one | `src/app/ui/accordion-nav.ts:27,41,59,74,89` | Five literal `<section>` blocks with `class="hd"` headers. `tests/branding-and-layout.test.mjs` "exactly the five sections, and no Shared Blocks" — `headerCount === 5`, all five names matched, no rendered "Shared Blocks". | Confirmed |
| layout.three-panel | narrow-viewport-notification | < 768px: dismissible "not optimized for small screens" notice; identical to phase-0; layout does not restructure | `src/app/ui/narrow-viewport-notice.ts` | `BREAKPOINT_PX = 768`; `narrow = window.innerWidth < 768` (updated on `resize`); `<div class="nvn" role="status">` with a `×` button; `NarrowViewportNoticeState` (`providedIn:'root'`, one `dismissed` signal, in-memory only). Rendered once in `app.ts` as flow content above `<router-outlet>` — adds no media query and no panel rule. Browser smoke @700px: notice shown; the three `.panel` elements stay in the DOM. | Confirmed |

---

## components/publication-metadata.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| publication.metadata | mustNever | Don't confuse publication metadata with a topic's metadata | `src/app/ui/publication-form.ts`, `src/app/core/publication-store.ts:73` | `publication-form.ts` binds only `store.meta().displayName` / `store.setMeta(...)`; `setMeta` writes `this.meta` only. Topic name/metadata are written by a separate path (`patchTopic`, store:135). They are separate signals and separate Panel 1 selections. | Confirmed |
| publication.metadata | default-selection-on-load | Publication form shown by default on a fresh load | `src/app/core/publication-store.ts:52` | `selection = signal<…>({ type: 'publication' })`. Browser smoke: `.panel-2 h1` reads "Publication" on first load with nothing clicked. | Confirmed |
| publication.metadata | form-not-markdown | Structured data → form, no Preview tab | `src/app/ui/editor-generic.ts:33`, `src/app/ui/publication-form.ts` | `@case ('publication') { <app-publication-form /> }` — `publication-form.ts` has a single `<input>` and no tab strip. Only `topic-editor.ts` renders tabs. | Confirmed |

---

## components/topic-hierarchy.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| topic.hierarchy | mustNever-1 | No hardcoded max depth | `src/app/core/tree.ts` | See system mustNever-1. `tests/tree.test.mjs` extends a tree to 6 levels with no rejection. | Confirmed |
| topic.hierarchy | mustNever-2 | move up/down must not change a topic's parent | `src/app/core/tree.ts:96-107` | `moveSibling` `reorder` only ever swaps two entries **within one `nodes` array** (`[next[idx], next[swapWith]] = …`); when the id is not at this level it recurses into `children` unchanged. `tests/tree.test.mjs` "swaps with the immediate sibling, never changes parent" — after a move, `b` still owns child `c`, and `b2` is still `a`'s child. | Confirmed |
| topic.hierarchy | mustNever-3 | Deleting a topic must not silently orphan/delete children without telling the author | `src/app/ui/topic-tree-node.ts:34-46`, `src/app/core/tree.ts:80-84` | Delete opens `<div class="confirm">` showing `{{ descendants() }} descendant topic(s)` before `doDelete()` calls `store.removeTopic`. `descendants()` = `store.descendantCount` = `countDescendants` (tree.ts:45). Browser smoke: confirm reads `Delete "Intro" and its 1 descendant topic?`. | Confirmed |
| topic.hierarchy | recursive-not-fixed-depth | Children at any node, any depth; no component assumes flat/N-level | `src/app/ui/topic-tree-node.ts` (self-recursive), `src/app/core/tree.ts` | `topic-tree-node.ts` renders `<app-topic-tree-node [topic]="child" [depth]="depth()+1" />` for each child — a standalone component referencing itself. `tests/tree.test.mjs` as above. | Confirmed |
| topic.hierarchy | add-child-topic | "Add child topic" nests one level beneath, appended last | `src/app/core/tree.ts:71-74`, `src/app/core/publication-store.ts` `addChild` | `addChildTopic`: `children: [...p.children, makeTopic(name)]`. `tests/tree.test.mjs` "appended as the LAST child, one level beneath". Browser smoke: the ＋ button on a node adds a nested child. | Confirmed |
| topic.hierarchy | move-reorders-siblings-only | Swap with immediate sibling; no-op at the ends, not an error | `src/app/core/tree.ts:99-102` | `const swapWith = direction === 'up' ? idx - 1 : idx + 1; if (swapWith < 0 || swapWith >= nodes.length) return [...nodes];` `tests/tree.test.mjs` "move up/down at the ends is a no-op, not an error". | Confirmed |
| topic.hierarchy | delete-warns-about-children | Show how many descendant topics will also be removed, before confirm | `src/app/ui/topic-tree-node.ts:36-40`, `src/app/core/tree.ts:45-49` | `@if (descendants() > 0) { Delete "…" and its {{ descendants() }} descendant topic{{ … }}? }`. Verified in browser smoke. | Confirmed |
| topic.hierarchy | topic-id-is-stable | id assigned once at creation, never changes (incl. move / child add-remove) | `src/app/core/ids.ts`, `src/app/core/tree.ts:18` | `makeTopic` sets `id: newId('topic')` once; no function reassigns `.id` (every op spreads `{ ...n, … }` keeping `id`). `tests/tree.test.mjs` "id is unchanged by move and by adding/removing children". | Confirmed |

---

## components/source-with-override.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| source.with-override | mustNever-1 | A topic's override must not remove/alter a source for any OTHER topic | `src/app/core/sources.ts:21-25` | `resolveSourcesForTopic` is pure and per-topic: `allSources.filter((s) => wanted.has(s.id))` returns a new array, never mutates `allSources` or any other topic. `tests/sources.test.mjs` "one topic's override does not affect another topic". | Confirmed |
| source.with-override | mustNever-2 | Don't require every topic to list sources — default is all | `src/app/core/sources.ts:22` | `if (topic.sourceOverride === null) return [...allSources];` — a topic with no override uses every source. `topic-editor.ts` defaults new topics with the "Use all publication sources" box checked. `tests/sources.test.mjs` "default-is-all-sources". | Confirmed |
| source.with-override | default-is-all-sources | No override ⇒ all; override ⇒ only those ids, replacing default | `src/app/core/sources.ts:21-25` | As above; `tests/sources.test.mjs` "override replaces the default entirely" + "an empty override list means no sources, not all sources". | Confirmed |
| source.with-override | override-references-real-sources-only | Bad ids are a Quality-Check finding, not enforced live | `src/app/core/sources.ts:29-34` (`danglingOverrideIds`), `src/app/core/quality-check.ts:52-66` | `danglingOverrideIds` returns override ids with no matching source; `quality-check.ts` `source-reference-resolution` uses it and names topic + bad id. Nothing in `topic-editor.ts` blocks typing/selecting. `tests/sources.test.mjs` + `tests/quality-check.test.mjs`. | Confirmed |

---

## components/persona-catalog.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| persona.catalog | mustNever-1 | No more than 2 personas | `src/app/core/publication-store.ts:161` | `if (this.personas().length >= MAX_PERSONAS) return;` See system mustNever-2. | Confirmed |
| persona.catalog | mustNever-2 | Don't derive a persona's id from its name — id is positional | `src/app/core/publication-store.ts:162-164` | `const id = \`persona-${this.personaSeq()}\`; this.personaSeq.update((n) => n + 1);` `patchPersona` explicitly excludes `id` from the patch type (`Partial<Pick<Persona,'name'|'summary'>>`, store:170). Browser smoke: ids are `persona-0,persona-1`. | Confirmed |
| persona.catalog | max-two-personas | Identical to phase-0: no third persona | `src/app/core/publication-store.ts:161`, `src/app/ui/personas-form.ts` | store guard + `@if (store.canAddPersona())` hides the button. Browser smoke confirms. | Confirmed |
| persona.catalog | five-fixed-dimensions | Dimensions only from this app's system.yaml fixedDimensions list | `src/app/core/models.ts:11`, `src/app/ui/personas-form.ts` | `FIXED_DIMENSIONS = ['Surface','Content','Context','Time','Trust']` (matches this app's `system.yaml:31` by value); `personas-form.ts` `dimensions = FIXED_DIMENSIONS`. Browser smoke: 5 dimension checkboxes per card. | Confirmed |
| persona.catalog | persona-id-is-positional | First = persona-0, second = persona-1; assigned once, unchanged on edit | `src/app/core/publication-store.ts:162` | As mustNever-2. `personaSeq` only increments (never decremented on remove — store:184 comment). | Confirmed |
| persona.catalog | publication-level-not-per-topic | Personas authored once, apply to every topic; no per-topic persona list | `src/app/ui/personas-form.ts`, `src/app/core/publication-refract.ts:68` | The only persona editor is `personas-form.ts` (Panel 1 "Personas"). `refractPublication` loops `pub.personas` for every topic — one shared list. `TopicNode` (`models.ts`) has no persona field. | Confirmed |

---

## components/variable-substitution.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| variable.substitution | mustNever-1 | Don't resolve/substitute a variable's value live while typing | `src/app/core/variables.ts`, `src/app/ui/markdown-view.ts` | `variables.ts` exports exactly `VARIABLE_REFERENCE_RE`, `referencedVariableNames`, `undefinedVariableNames` — no substitute/render/resolve function. `tests/variables.test.mjs` "the module exposes NO substitute/render/resolve function" asserts the export list. Browser smoke: Preview of `Uses {{api_name}} here.` renders `<p>Uses {{api_name}} here.</p>` — token shown raw. | Confirmed |
| variable.substitution | mustNever-2 | No topic-local variables — all are publication-level | `src/app/ui/variables-form.ts`, `src/app/core/models.ts` | Variables are created only by `store.addVariable()` (publication level). `TopicNode` has no `variables` field. `topic-editor.ts` has no variable-definition control. | Confirmed |
| variable.substitution | reference-syntax | `{{variable_name}}` matching `name` exactly, case-sensitive | `src/app/core/variables.ts:17` | `VARIABLE_REFERENCE_RE = /\{\{([A-Za-z0-9_]+)\}\}/g`; `undefinedVariableNames` compares against `new Set(variables.map(v => v.name))` (no case-folding). `tests/variables.test.mjs` "reference-syntax is case-sensitive" (`{{alpha}}` ≠ defined `Alpha`). | Confirmed |
| variable.substitution | form-not-markdown | Structured data → form, no Preview tab | `src/app/ui/editor-generic.ts`, `src/app/ui/variables-form.ts` | `@case ('variables') { <app-variables-form /> }`; `variables-form.ts` renders name/value inputs, no tabs. | Confirmed |

---

## components/editor-generic.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| editor.generic | mustNever-1 | No Preview tab for structured data | `src/app/ui/editor-generic.ts:15-17`, the four `*-form.ts` files | Only `topic-editor.ts` has a tab strip (`Metadata / Editor / Preview`, topic-editor.ts:24-30). `publication-form.ts`, `variables-form.ts`, `sources-form.ts`, `personas-form.ts` render plain forms with no tabs. | Confirmed |
| editor.generic | mustNever-2 | No modal dialog — every edit inline in Panel 2 | `src/app/ui/editor-generic.ts` | Every `@case` branch is an inline child component. See system mustNever-3 + `tests/branding-and-layout.test.mjs`. | Confirmed |
| editor.generic | no-dialogs-anywhere | Zero modal dialogs; all five types open inline | `src/app/ui/editor-generic.ts`, `src/app/ui/settings-page.ts` | `editor-generic.ts` `@switch` has all five cases inline; even Settings is a routed page, not an overlay. Dialog-marker scan → none. | Confirmed |
| editor.generic | default-on-load | With nothing selected, Panel 2 shows publication.metadata's form | `src/app/core/publication-store.ts:52`, `src/app/ui/editor-generic.ts:33` | `selection` default `{ type: 'publication' }` → `@case ('publication') { <app-publication-form /> }`. Browser smoke confirms. | Confirmed |

---

## components/guide-static.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| guide.static-per-selection | mustNever | Don't generate Guide content dynamically or react to the content being edited | `src/app/ui/guide-panel.ts:34`, `src/app/core/guide-content.ts` | `block = computed(() => GUIDE_BLOCKS[this.store.selection().type])` — keyed only by selection **type**; never reads topic content, names, or values. `GUIDE_BLOCKS` is a static const object. | Confirmed |
| guide.static-per-selection | five-fixed-blocks | Exactly five blocks, one per type; no sixth, no per-topic variation | `src/app/core/guide-content.ts` | `GUIDE_BLOCKS: Record<SelectionType, …>` with keys `publication, topic, variables, sources, personas`. `tests/branding-and-layout.test.mjs` asserts `Object.keys(GUIDE_BLOCKS).sort()` is exactly those five. | Confirmed |

---

## components/quality-check.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| quality-check.four-checks | mustNever-1 | Don't enforce any of the four checks live, while typing | `src/app/ui/quality-check-panel.ts:33-36`, `src/app/core/quality-check.ts` | `runQualityCheck` is called only from `run()` behind the "Run checks" button; `report` is `signal<QualityReport \| null>(null)` until then. No input/keystroke handler calls it. `variable.substitution` / `source.with-override` editors do no live validation. | Confirmed |
| quality-check.four-checks | mustNever-2 | Don't allow refraction to run if persona-exists fails | `src/app/core/quality-check.ts:93`, `src/app/core/publication-refract.ts:55-57`, `src/app/ui/refract-bar.ts` | `refractionAllowed: personaExists.passed`. `refractPublication`: `if (pub.personas.length === 0) return { cells, ran: false };`. `refract-bar.ts` `canRefract = hasApiKey && hasPersonas && hasTopics`, button `[disabled]`. `tests/refraction.test.mjs` "refractPublication does nothing when there are no personas" + `tests/quality-check.test.mjs` "persona-exists … blocks refraction". | Confirmed |
| quality-check.four-checks | check: metadata-completeness | Every topic has at least a name | `src/app/core/quality-check.ts:46-52` | `const noName = topics.filter((t) => t.name.trim().length === 0)`; failure line `Topic ${t.id} has no name.` `tests/quality-check.test.mjs` "names the specific unnamed topic". | Confirmed |
| quality-check.four-checks | check: source-reference-resolution | Every sourceOverride id resolves to a real source | `src/app/core/quality-check.ts:54-66` | Uses `danglingOverrideIds(t, pub.sources)`; failure names topic + `"${badId}"`. `tests/quality-check.test.mjs` "names the topic and the bad source id". | Confirmed |
| quality-check.four-checks | check: variable-resolution | Every `{{name}}` in any topic's content is defined | `src/app/core/quality-check.ts:68-80` | Uses `undefinedVariableNames(t.content, pub.variables)`; failure names topic + `{{${name}}}`. `tests/quality-check.test.mjs` "names the topic and the undefined variable". | Confirmed |
| quality-check.four-checks | check: persona-exists | At least one persona before refraction is allowed | `src/app/core/quality-check.ts:82-92` | `passed: pub.personas.length > 0`; drives `refractionAllowed`. Tests as mustNever-2. | Confirmed |
| quality-check.four-checks | check-report-is-per-item | A failed check names the specific topic / source id / variable / persona gap | `src/app/core/quality-check.ts:46-92` | Every `failures` array entry is built per offending item (`.map`/`for` over the specific list). `tests/quality-check.test.mjs` "multiple failures produce one line each, none generic". Browser smoke: 4 checks render, failing ones list items. | Confirmed |

---

## components/refraction-publication-level.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| refraction.publication-level | mustNever-1 | Don't require a separate action per topic | `src/app/core/publication-refract.ts:44-105`, `src/app/ui/refract-bar.ts` | `refractPublication` is the single entry point; `refract-bar.ts` has one "Refract publication" button calling `run()` once. `tests/refraction.test.mjs` "a single call refracts every (topic × persona) pair" (6 calls from 1 invocation). | Confirmed |
| refraction.publication-level | mustNever-2 | Don't invent a value not in a topic's content + resolved sources | `src/app/core/publication-refract.ts:80-99` | Post-call `checkGrounding`; ungrounded → `{ type: 'ungrounded-claim' }`, text discarded. Same grounding module as phase-0 (`grounding.ts` copied verbatim). `tests/refraction.test.mjs` "an ungrounded model output is surfaced as an error". | Confirmed |
| refraction.publication-level | mustNever-3 | Don't depend on a specific model provider | `src/app/core/publication-refract.ts` (imports `import type { RefractionTransport }` only), `src/app/ui/refract-bar.ts` (`transportFor(store.provider())`) | No vendor named in the fan-out; transport resolved at call time from the registry. `tests/refraction.test.mjs` passes a fake transport. | Confirmed |
| refraction.publication-level | one-action-many-calls | One click → one compiler call per (topic × persona) pair, possibly many | `src/app/core/publication-refract.ts:66-104` | Nested `for (const topic of topics) { for (const persona of pub.personas) { … refractOnce(…) } }`. `tests/fixture-parity.test.mjs` "exactly topics × personas calls" (5 × 2 = 10). | Confirmed |
| refraction.publication-level | resolved-sources-per-topic | Each call uses the topic's resolved source set (default or override) | `src/app/core/publication-refract.ts:67` | `const resolvedSources = resolveSourcesForTopic(topic, pub.sources);` used for that topic's calls and its grounding check. `tests/refraction.test.mjs` "an override narrows what a topic call sees" (empty override → prompt shows "(none provided)"). | Confirmed |
| refraction.publication-level | same-failure-behavior-as-phase-0 | Each call = retry-once then a visible structured error; one failure doesn't block others | `src/app/core/refraction.ts` (`refractOnce`, copied from phase-0), `src/app/core/publication-refract.ts:71-101` | `refractOnce` loop `for (let attempt = 0; attempt < 2; …)`; the fan-out `try`s each pair and stores the error cell, never `throw`s out of the loop. `tests/refraction.test.mjs` "retries exactly once, then returns a structured error" + "one failing pair does not block the others". | Confirmed |

---

## components/branding-rename.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| branding.rename | mustNever-1 | Don't hardcode a product name in this component's logic | `src/app/brand/brand.ts` | The only literal `'Catenator'` in `src/`. `tests/branding-and-layout.test.mjs` "the product name literal is in exactly one file (brand.ts)" scans every `.ts/.html/.css` under `src/`. | Confirmed |
| branding.rename | mustNever-2 | Don't leave a prior product name anywhere in the built app | `src/app/brand/brand.ts` (`PRIOR_NAMES`) | `tests/branding-and-layout.test.mjs` "no retired product name anywhere in src" scans for `'Syntaxia'` / `'Syntaxia Studio'` outside brand.ts → none. | Confirmed |
| branding.rename | scan-for-prior-names | Search for hardcoded prior names; report every occurrence | `tests/branding-and-layout.test.mjs` | The scan test IS the enforcement and passes (0 occurrences). | Confirmed |
| branding.rename | single-source-of-truth | Product name read from config, never duplicated as a literal | `src/app/brand/brand.ts`, `src/app/app.ts` (`BRAND`, `BRAND_LINE`, `BRAND_TITLE`) | `index.html` `<title>` is a pre-hydration placeholder (the tagline, not the product name); `App` sets the real title from `BRAND_TITLE` (`app.ts` constructor). Test confirms one literal. Browser smoke: `document.title` = "Catenator — Creating and Refracting — Basic Publication". | Confirmed |
| branding.rename | report-changes | Every file changed by the rename is reported | this report + `output/BUILD_REPORT.md` | Fresh build — `BUILD_REPORT.md` lists every generated file by component. | Confirmed |

---

## components/style-visual-theme.yaml

| Component | Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|---|
| style.visual-theme | mustNever-1 | Don't change pipeline / hierarchy / refraction / Quality Check as part of styling | `src/styles.css`, component `styles:` blocks | Styling is CSS only (`styles.css` + `@Component` `styles` arrays). No `.ts` logic file is a styling artifact; the core modules were not touched for presentation. | Confirmed |
| style.visual-theme | mustNever-2 | No import/live dependency on the reference application's codebase | `src/styles.css`, `src/app/ui/icon-registry.ts` | Token values are static in `styles.css`; the one SVG mark is inline in `icon-registry.ts`. `tests/branding-and-layout.test.mjs` scans `src/` for `syntaxia-studio` / `eisyntaxia_repos` → none. Nothing is `@import`ed or fetched at runtime. | Confirmed |
| style.visual-theme | mustNever-3 | No new icon library / dependency without first checking phase-0's tokens | `output/package.json` | Dependencies are `@angular/*`, `rxjs`, `tslib` only — identical to phase-0. `tests/branding-and-layout.test.mjs` asserts no dependency matching `/icon|feather|lucide|fontawesome|heroicons/i`. The one mark is inline SVG reused from phase-0. | Confirmed |
| style.visual-theme | reuse-phase-0-tokens-first | Reuse phase-0's committed design-system tokens directly before the external fallback | `src/styles.css` | `styles.css` is phase-0's committed token file, reused verbatim (`:root` custom properties — `--rail-bg`, `--accent-blue`, `--badge-pass-bg`, Roboto/Roboto Mono/Roboto Slab, radii). The header comment records the reuse. No inspection of the external `syntaxia-studio` path was done. | Confirmed (reuse), reference inspection not performed |
| style.visual-theme | standalone-styling | All styling values committed in this app's codebase; nothing loads from a reference app at runtime | `src/styles.css`, `src/app/ui/icon-registry.ts` | Both committed under `src/`. Runtime scan → nothing external. | Confirmed |

---

## Overall verdict

**Fully compliant** against every `mustNever` and `micro` rule across all twelve
components, with two items that a headless build cannot fully verify — both
expected, neither a gap:

1. **A live model call** (`refraction.publication-level`) — the whole contract is
   unit-tested with a fake transport (fan-out count, per-topic resolved sources,
   retry-once, structured errors, one-failure-isolation, grounding gate); no real
   Claude/Gemini request was made. `npm run verify:refraction` runs the real
   path on demand and this run reached the provider end-to-end.
2. **`style.visual-theme` reference inspection** — per `reuse-phase-0-tokens-first`
   the build reused phase-0's already-committed tokens directly; the external
   reference path (`…\syntaxia-studio`, an absolute local path) was not inspected.

The `check.grounding` logic carried over from phase-0 is a deterministic
heuristic (specific-claim membership + disclosure exemption); it satisfies the
stated grounding constraint and is tested, but by nature could over- or
under-flag an unusual phrasing.

`41/41` tests pass (including 5 fixture-parity assertions), the production build
is clean (256.5 kB initial), and the workspace — five accordion sections, the
generic inline editor with topic tabs, the arbitrary-depth tree with
add/child/move/delete-with-warning, the two-persona cap with positional ids,
the four Quality Checks, and the < 768px notice — was exercised in a real
browser. **No "Not found" or "Conflicting" rows.**
