# Build-compliance report — phase-0-single-topic (component update)

- **Spec:** `apps/phase-0-single-topic/specs/`
- **Build mode:** COMPONENT UPDATE — `COMPONENTS = ["components/input-mode-dual.yaml", "components/state-topic-refraction.yaml"]`. A project already exists at `apps/phase-0-single-topic/output/`; only the code responsible for these two components was touched.
- **Config:** `apps/phase-0-single-topic/prompts/build-config.yaml` — now carries a `defaultFixture` block pointing at `fixtures/promotions/{topic.md, sources-freetext-markdown.md, personas-freetext.md}`.
- **Fixture:** `FIXTURE_DIR = fixtures/promotions/` — exists with `topic.md`, `sources-freetext-markdown.md`, `personas-freetext.md`, `expected-parsed.yaml`. The parity check now runs against this directory instead of `fixtures/rate-limiting/`.
- **Generated:** 2026-09-08 10:52 (local)
- **Verification:** `npm test` → **31/31 pass** (Node 22.23.2) — the promotions parity suite + a new `default-fixture.test.mjs` (6 assertions). `npm run build` (production) → **clean, ~260 kB initial**. Browser (headless): on first load Steps 1–3 are pre-populated from the promotions fixture; `/sources`, `/personas`, `/refract` are all reachable directly (gating satisfied by the seeded data); replacing the topic still cascades-invalidates downstream; Start-over re-seeds.

## Changed this run

| File | Component | Change |
|---|---|---|
| `src/app/core/default-fixture.ts` (new) | `state.topic-refraction` | Build-time materialization of `build-config.yaml`'s `defaultFixture` — the three fixture files embedded as strings + a pure `initialStateFromFixture()` that parses them through the free-text parsers. Generated by `scripts/gen-default-fixture.mjs` (new). |
| `src/app/core/session-store.ts` | `state.topic-refraction` | `constructor()` calls `seedFromDefaultFixture()` (session-store.ts:28-30, 114-119); `reset()` re-seeds (session-store.ts:140). |
| `tests/parse-freetext.test.mjs` | `input-mode.dual` | Parity check `FIX` path → `fixtures/promotions`; `loadExpected()` YAML reader accepts `>` (not only `>-`) and no longer requires a `topicText` key. |
| `tests/default-fixture.test.mjs` (new) | `state.topic-refraction` | 6 assertions for `initializes-from-default-fixture`. |
| `fixtures/promotions/expected-parsed.yaml` | `input-mode.dual` fixture data | One folded-scalar line re-wrapped so `review-before-apply` is not split across a hyphen (YAML `>` folds `review-before-\n  apply` to `review-before- apply`, which disagreed with the source `.md`). No change to any expected value. |

No runtime parser code (`parse-freetext.ts`) changed — `input-mode.dual`'s only change is the parity-check target directory.

---

## components/state-topic-refraction.yaml — full rule check

| Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|
| mustNever | Lose or overwrite author-entered data between steps | `src/app/core/session-store.ts` | Setters run only on explicit author actions; navigation never clears. Seeding happens **once, in the constructor, before any author entry** (session-store.ts:28-30) — it cannot overwrite author data because there is none yet. `setTopic`'s existing cascade is unchanged and still fires only on a real topic change. | Confirmed |
| mustNever | Persist data beyond the current session | `src/app/core/session-store.ts` | Signals only; no `localStorage`/`sessionStorage`/`indexedDB` in `src/`. The seed is re-derived from bundled constants on every load — it is a starting value, not persisted state. A refresh restarts from the same fixture, matching `persistenceScope` ("no persistence across page refresh"). | Confirmed |
| fields | topicText / sources / personas{id,name,summary,dimensions[]} max 2 / refractedOutputs map | `src/app/core/models.ts`, `src/app/core/session-store.ts` | Shapes unchanged. Seeded personas go through `setPersonas` (session-store.ts:117), which still caps at `MAX_PERSONAS` and assigns positional ids. | Confirmed |
| **initializes-from-default-fixture** | On first load, Steps 1–3 pre-populated from `build-config.yaml`'s `defaultFixture` (topicText, sources, personas); a starting value, not locked — author can edit/replace exactly as if typed; no mandatory/validation difference | `src/app/core/default-fixture.ts`; `src/app/core/session-store.ts:28-30, 114-119, 140` | `default-fixture.ts` embeds the three files named by `defaultFixture` and exposes `initialStateFromFixture()`, which runs `parseSourcesFreetext` / `parsePersonasFreetext` on the `sources`/`personas` strings — **the same parsers the free-text input mode uses** — so seeded values are byte-identical to an author pasting that text. `seedFromDefaultFixture()` sets `topicText` directly (avoiding `setTopic`'s cascade), then `setSources`, then `setPersonas`. No new validation branch anywhere: `hasTopic`/`hasSources`/`hasPersonas`, the guards, and every step's Continue action are unchanged and do not know whether a value was seeded. `tests/default-fixture.test.mjs`: "seeded values are identical to parsing the same text as free-text input", "pre-populates all of Steps 1-3", "a fixture that fails to parse yields an empty section, not a throw". Browser: `/topic` textarea, `/sources` title field, and both `/personas` name fields are pre-filled on first load; all downstream routes reachable. | Confirmed |
| persona-id-is-positional | First persona = persona-0, second = persona-1; assigned once, never changes on edit | `src/app/core/session-store.ts` `setPersonas` (`id: existing[i]?.id ?? \`persona-${this.nextId()}\``) + `nextId()` | Unchanged. Seeded personas are assigned `persona-0` / `persona-1` via the same path (`personaSeq` starts at 0). Browser: seeded persona cards carry `persona-0` / `persona-1`. `tests/default-fixture.test.mjs` checks the two seeded persona names in order. | Confirmed |
| refracted-output-map | Keyed by personaId, one entry per persona, never merged | `src/app/core/session-store.ts` `putRefraction` (`new Map(...)` then `.set`) | Unchanged. Seeding sets `refractedOutputs` to a fresh empty `Map` (via `setPersonas`). | Confirmed |

---

## components/input-mode-dual.yaml — full rule check

| Rule ID | Rule (brief) | Implemented in (file:line) | Evidence | Status |
|---|---|---|---|---|
| mustNever | Two modes must not produce divergent data | `src/app/core/parse-freetext.ts` + `steps/sources-step.ts` / `personas-step.ts` | Unchanged. Both modes call `store.setSources` / `store.setPersonas` with the same shape. `tests/parse-freetext.test.mjs` parity assertions now run against `fixtures/promotions/` and pass. | Confirmed |
| mustNever | Don't split one labeled record into multiple entries | `src/app/core/parse-freetext.ts` `parseSourcesFreetext` returns `sources: [one]` | Unchanged. `tests/parse-freetext.test.mjs` "one labeled record stays ONE source" (promotions fixture). | Confirmed |
| mustNever | Don't block advancement after a successful free-text parse | `steps/*-step.ts` shared `next()` path | Unchanged. | Confirmed |
| mustNever | Don't infer/add a field value not stated in the free-text | `src/app/core/parse-freetext.ts` `asDimensionLine` (any non-dimension token disqualifies the line), `parseSourcesFreetext` (missing section → error) | Unchanged. `tests/parse-freetext.test.mjs` "parse-only-what-is-stated" (two cases). | Confirmed |
| mustNever | No structured-form field without both a visible label and placeholder | `steps/sources-step.ts` / `personas-step.ts` | Unchanged — every field has `<label>` + `placeholder`. | Confirmed |
| mustNever | Don't keep `##` / structural quote marks in a parsed value | `src/app/core/parse-freetext.ts` `h2Sections` + `flatten` | Unchanged. `tests/parse-freetext.test.mjs` checks no `##` and no wrapping quotes on parsed promotions values. | Confirmed |
| mode-parity | Free text parses into the exact fields the form writes | `src/app/core/parse-freetext.ts` | `tests/parse-freetext.test.mjs` "parity: sources …" / "parity: personas …" — parsed promotions output deep-equals `expected-parsed.yaml`. | Confirmed |
| sources-format | `## Title` / `## Source` / `## Description` H2 sections | `src/app/core/parse-freetext.ts` `parseSourcesFreetext` (`pick('title'|'source'|'description')`) | Unchanged; parity against `fixtures/promotions/sources-freetext-markdown.md` passes. | Confirmed |
| personas-format | `## <name>` = name; next paragraph = summary; trailing comma-list vs `system.yaml` fixedDimensions = dimensions, matched by name | `src/app/core/parse-freetext.ts` `parsePersonasFreetext` + `asDimensionLine` (matches `FIXED_DIMENSIONS`) | Unchanged; parity against `fixtures/promotions/personas-freetext.md` passes — `"Context, Trust"` → `["Context","Trust"]`, `"Content, Time"` → `["Content","Time"]`. | Confirmed |
| **parity-check-required** | Run the parser against `fixtures/promotions/` (topic.md, sources-freetext-markdown.md, personas-freetext.md) and confirm the parsed result matches `expected-parsed.yaml` exactly | `tests/parse-freetext.test.mjs` (FIX → `fixtures/promotions`) | The test reads the three promotions fixture files, parses them, and asserts deep-equality against a reader of `fixtures/promotions/expected-parsed.yaml` — entry counts, field values, and canonical dimension order. **Run as part of `npm test`** — all parity assertions pass (31/31). One YAML wrap in `expected-parsed.yaml` was corrected so `review-before-apply` is not folded to `review-before- apply`; no expected value changed. | Confirmed |
| advance-after-parse | Continue behaves identically to the form path after a valid parse | `steps/*-step.ts` shared `next()` | Unchanged. | Confirmed |
| parse-only-what-is-stated | Never infer/guess/auto-complete a field the input didn't state | `src/app/core/parse-freetext.ts` | Unchanged. `tests/parse-freetext.test.mjs` "a line with a non-dimension token is NOT treated as dimensions" + "a dimension not written is not selected". | Confirmed |
| field-labels-and-placeholders | Every form field shows a visible label + placeholder | `steps/sources-step.ts` / `personas-step.ts` | Unchanged. | Confirmed |
| clean-value-extraction | Parsed values contain only the value — no `##`, labels, colons, structural quotes | `src/app/core/parse-freetext.ts` `flatten` + `h2Sections` | Unchanged. `tests/parse-freetext.test.mjs` asserts no `##` / wrapping quotes on the promotions parse. | Confirmed |
| downloadable-template | Free-text mode offers a downloadable markdown template (headers only) | `src/app/ui/freetext-template.ts` `SOURCES_TEMPLATE` / `PERSONAS_TEMPLATE` + `downloadText` | Unchanged — templates are section headers only. | Confirmed |

---

## Other components (not in COMPONENTS this run)

Per RUN_TEMPLATE.md COMPONENT UPDATE MODE ("Read only the files listed in
COMPONENTS … Touch only files responsible for these components"), the remaining
nine components — `layout.three-panel`, `gating.linear-sequential`,
`byok-compiler.contract`, `interrupt.conditional-api-key`, `check.grounding`,
`delivery.request-response`, `step.introduction`, `branding.rename`,
`style.visual-theme` — were **not modified and not re-verified this run**. Their
last full rule-by-rule verification stands in
`apps/phase-0-single-topic/reports/compliance-2026-09-07-1143.md` (all Confirmed;
two "Present but unverified": a live model call, the style-reference inspection).

Note on `byok-compiler.contract`: `scripts/verify-refraction.mjs` still reads
`fixtures/rate-limiting/` (which still exists). It is not responsible for either
updated component and was left untouched. `gating.linear-sequential` is
unaffected in logic — with Steps 1–3 seeded, `earliestIncomplete` simply returns
`refract` on first load instead of `topic`, which is the intended effect of the
new fixture rule, not a gating change.

---

## Overall verdict

**Compliant.** Both updated components pass every `mustNever` and `micro` rule:

- `state.topic-refraction.initializes-from-default-fixture` — implemented as a
  build-time embed of `build-config.yaml`'s `defaultFixture` files, seeded once
  at construction (and on Start-over) through the exact free-text parsers, with
  no new validation path. Verified by unit tests and in a real browser.
- `input-mode.dual.parity-check-required` — the parity suite now targets
  `fixtures/promotions/` and passes against its `expected-parsed.yaml`.

`31/31` tests pass, the production build is clean, and the seeded first-load
flow was exercised in a real browser. **No "Not found" or "Conflicting" rows.**
